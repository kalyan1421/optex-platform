# Runbook moved

The local + production runbook now lives at the repo root:
[RUNBOOK.md](../RUNBOOK.md).

This file is kept only so old links don't 404 — it no longer duplicates the
content (the previous version here was stale: wrong dev ports, and it
described the `supabase start` CLI flow instead of the Docker-Compose-first
workflow this repo actually uses).
