# OPTEX — Test Plan

**Date:** 2026-08-13 · Written against `docs/FEATURE-STATUS.md`'s 73 ✅-done features.

Starting point: 3 API test files (34 Jest+supertest e2e tests) and 4 Playwright files (10 tests) — covering checkout's signed-out boundary, cart guest-merge, cart empty states, the appointments auth gate, and SPEC-06 order cancellation end to end. Everything else "done" per FEATURE-STATUS was unverified by anything automated. Two passes later: 8 API test files (96 tests) and 5 Playwright files (13 tests). `apps/admin` still has zero tests — it's Tier 3, not attempted yet.

## Method

Risk-tiered, not feature-by-feature: 73 items is too many to test with equal weight, and most of the risk concentrates in a handful of areas — money, auth, data isolation, and concurrency. Tier 1 gaps were closed first; Tier 2+ are scoped but not yet built, listed as backlog below rather than silently deferred.

## Tier 1 — Critical (money, auth, data integrity, concurrency): closed this pass

| Area                                  | Gap found                                                                                                                                                                                               | Test added                                                                                                                                                                          | Result                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Appointment booking**               | Zero coverage. `assertSlotBookable()` is a SELECT-then-INSERT with no DB constraint — SPEC-04 R1's own acceptance criterion ("a concurrency test exists and runs in CI") had never been written         | `appointments.e2e-spec.ts`, 12 tests: booking, slot-grid validation, ownership (404 not 403), cancel/reschedule, admin reschedule, RBAC — and a 5-way concurrent-booking race       | **Found a real bug.** The race was live: nothing stopped two concurrent bookings for the same slot. Closed with migration `0014` (partial unique index on `appointments(branch_id, scheduled_at) where status <> 'cancelled'`) + a `23505` catch in the service. Re-verified: exactly 1 of 5 concurrent requests now succeeds, enforced by the database, not request timing |
| **Order history / detail / tracking** | Zero regression coverage protecting two real bugs fixed earlier this session (`/profile` filtered by auth-user-id instead of `customers.id`; tracking selected a nonexistent `shipping_address` column) | `orders.e2e-spec.ts`, 10 tests: list/detail/tracking, ownership isolation, tracking-stage correctness per status, cancelled-order stage rendering                                   | Passing. Protects both fixes from silently regressing                                                                                                                                                                                                                                                                                                                       |
| **Prescriptions**                     | Zero coverage on HEALTH data — ownership isolation, signed-URL expiry, upload validation, admin path                                                                                                    | `prescriptions.e2e-spec.ts`, 10 tests: upload validation (MIME, 10MB ceiling), ownership isolation on list/download (404 not info-leaking), admin list/download/status-toggle, RBAC | **Found a real infra bug.** The local `supabase-storage` container was down — upload had been silently broken in local dev. Restarted it; not a code defect, but prescription upload had literally never been exercised locally until this pass caught it                                                                                                                   |
| **Authenticated checkout**            | Only the signed-out redirect was tested (`smoke.spec.ts`). The actual authenticated path — form validation, `POST /checkout`, `place_order` committing a real row — was unverified                      | `checkout-authenticated.spec.ts`, 3 tests: reaches the form (not redirected), missing-fields validation, full form submission reaching the payment-initiation boundary              | Passing. Stops at the M-Pesa/Pesapal boundary deliberately — both are unconfigured in local dev (`apps/api/.env` has empty `MPESA_*`), so a completed payment can't be exercised here. The test asserts it reaches an M-Pesa-specific error, which is the signal that checkout itself succeeded                                                                             |
| **Admin RBAC**                        | Only incidentally covered via cancellation-module 403 checks                                                                                                                                            | Extended via the appointments and prescriptions suites' own admin describe blocks                                                                                                   | Every admin route touched by new tests now has an explicit non-admin-gets-403 case                                                                                                                                                                                                                                                                                          |

**Net: 34 → 66 API tests, 10 → 13 Playwright tests. Two real bugs found and fixed** (the appointment double-booking race, live in production code; the local storage container being down, an infra issue). All 79 tests green; full workspace typecheck and prettier clean.

## Tier 2 — High (core UX, revenue-adjacent): closed this pass

| Area                                        | Gap found                                                                                                                                                                                                                                                                   | Test added                                                                                                                                                                                      | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Cart — line items and promo codes**       | Only "add" was tested (`smoke.spec.ts`, browser-level). Update/remove, the increment-on-re-add path, cross-customer isolation, and every promo-code rule (unknown/expired/exhausted codes, percent vs. fixed discount, clamping, VAT-on-post-discount-base) were unverified | `cart.e2e-spec.ts`, 18 tests, money-math asserted to the cent                                                                                                                                   | **Found two test-authoring bugs while writing it**, not production bugs: (1) `afterAll` deleted seeded products before the cart lines referencing them, tripping the `ON DELETE RESTRICT` FK and corrupting the next run's fixtures — reordered to cart-lines-then-products-then-category; (2) a PostgREST batch insert with heterogeneous row shapes sent explicit `NULL` for a column one row set and the others omitted, bypassing its `NOT NULL DEFAULT` — fixed by setting it explicitly on every row. Also confirmed and pinned down a real API inconsistency: cart's ownership check returns 403 (not the 404-either-way pattern orders/appointments/prescriptions use), which does confirm an item id's existence to a non-owner — low severity (UUIDs are unguessable) but worth knowing, not silently changed |
| **Reviews — create, moderation, aggregate** | Zero coverage. `ReviewsService` had no verification that a `pending` review stays off the public list, that a customer can review a product at most once, or that the rating aggregate computes correctly                                                                   | `reviews.e2e-spec.ts`, 10 tests: pending-invisible-until-approved, duplicate-review 409, rating bounds, admin list/filter/moderate, RBAC, aggregate math (including a 2-review rounded average) | Passing. Also confirmed `moderate()` on a nonexistent review returns 409, not 404 — a real, if minor, inconsistency worth knowing about rather than assuming                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

**Net: 66 → 96 API tests.** All passing, twice in a row (fixture idempotency re-verified after the two bugs above).

## Tier 2 remainder — not yet built

| Area                    | Why it matters                                                                                                                                                                                                                                                                                  | Suggested test type                                                                                                         |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Full paid-checkout path | `checkout-authenticated.spec.ts` stops at the payment boundary by necessity — M-Pesa/Pesapal are both unconfigured in local dev. If Daraja/Pesapal sandbox credentials become available, extend it (or add an API-level test using mocked provider responses) to reach a genuinely `paid` order | API e2e with a mocked `MpesaService`/`PesapalService`, or Playwright against real sandbox creds if the client provides them |

## Tier 3 — Medium (admin panel): zero coverage, unchanged by this pass

`apps/admin` has no test infrastructure at all — no Playwright config, no fixtures. Before writing admin tests, the panel needs its own Playwright project (mirroring `apps/web`'s, pointed at :1113) and an authenticated-admin fixture. Candidates once that exists, in order:

1. Orders — status transitions, the direct-cancel flow (SPEC-06 R3), the paid-order acknowledgement dialog
2. Products — CRUD + image upload
3. Payments — reconcile, the "Needs attention" tab
4. Reviews moderation

## Tier 4 — Low: intentionally not tested

Static marketing sections, content pages, policy pages — no logic to regress. Per this plan's "what to cover" principle, testing them would be padding, not coverage.

## Backlog note on scope

Tier 1 and Tier 2 are now closed end to end (96 API tests, 13 Playwright, all green twice in a row). What remains: the full paid-checkout path (blocked on payment-provider sandbox credentials or mocking, not effort), and Tier 3 (`apps/admin` — zero tests, needs its own Playwright project before anything else there can be tested).
