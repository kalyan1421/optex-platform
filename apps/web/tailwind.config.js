// Tailwind v4 — configuration has moved to app/globals.css (@theme block).
// This file is kept as an empty stub to avoid breaking any tooling that
// expects it to exist. Safe to delete once all IDEs/linters are updated.
